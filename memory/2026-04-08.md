## 2026-04-08

- Refined the mobile bathymetry presentation toward a papercut chart style:
  - moved the stationary/compass info into the bottom sheet header
  - strengthened inland contour layering/shadows
  - improved ocean raster contour layering
  - made contour-source selection include `lagos_contours` alongside regional survey sources instead of only as a no-primary fallback
- Added better tap preference logic so survey-backed contours beat coarse fallback data when multiple sources overlap.
- Recovered and published new live contour coverage on the Martin tile server:
  - Ohio (`oh_contours`) is live and publicly verified
  - Illinois (`il_contours`) is live and publicly verified
  - refreshed normalized PMTiles for NH/FL/MA/QC/OH and restarted Martin so the live schema is consistent
- Fixed the ArcGIS downloader so object-id batching no longer silently succeeds when services ignore `objectIds`.
- Added adaptive offset-pagination backoff for brittle ArcGIS services; Illinois now downloads much deeper than before and normalizes/tile-builds cleanly.
- Fixed the survey tiler to emit newline-delimited GeoJSON for Tippecanoe `--read-parallel`, which removed the old parser warnings on large contour builds.
- Added fallback `depth_ribbon` polygon derivation for line-only bathymetry sources:
  - Illinois rebuild now derives `3,402` ribbons and writes a richer `il_contours.pmtiles` (`11.2 MB` vs the older `4.3 MB` tile)
  - the app fill renderer now treats `depth_ribbon` like a papercut layer, so line-only lakes can show more depth-colour changes
- Repaired the vector ocean bathymetry path:
  - updated GEBCO OPeNDAP to the live CEDA endpoint
  - built local `ocean_contours.pmtiles`
  - registered `ocean_contours` in local Martin config
  - kept vector ocean rendering behind the existing env flag until the tile is published to the live server
- Finished the live tile publish on `24.199.80.77`:
  - synced `il_contours.pmtiles` and `ocean_contours.pmtiles` to the droplet
  - fixed Martin's crash loop by removing missing `mb_contours` / `sk_contours` references from `infra/martin/config.yaml`
  - added the `/tiles` bind mount to production compose and force-recreated Martin
  - verified Martin healthy with live `200` responses for:
    - `/catalog`
    - `/il_contours`
    - `/ocean_contours`
    - sample vector tiles `il_contours/5/8/12` and `ocean_contours/4/4/5`
  - switched nginx to a temporary HTTP-only fallback because the prod certbot volume has no TLS certs mounted right now; raw IP tile delivery works again while DNS/certs are fixed later
- Built a real US/Canada inland coverage matrix at `/Users/Ashar/Documents/fish/docs/us-canada-bathymetry-coverage-matrix.md` and used it to drive the next expansion wave.
- Extended live inland survey coverage again:
  - Connecticut (`ct_contours`) is now published and publicly verified
  - Indiana (`in_contours`) is now published and publicly verified
  - honest live survey-backed inland jurisdictions are now `16` total:
    - US: `CT, FL, IL, IN, MA, MI, MN, MT, NE, NH, OH, VT, WA`
    - Canada: `AB, ON, QC`
- Recovered the real Indiana production data source:
  - `https://gisdata.in.gov/server/rest/services/Hosted/Lake_Bathymetry_RO/FeatureServer`
  - downloaded `10,260` statewide contour features
  - normalized them into the OpenCatch schema and built `in_contours.pmtiles` (`12.4 MB`)
- Improved acquisition tooling for future expansion:
  - `download_all_states.py` now points Indiana at the live `gisdata.in.gov` service instead of the dead `maps.indiana.edu` host
  - `download_all_states.py` now recursively splits failing ArcGIS `objectIds` batches instead of aborting immediately
- North Dakota is partially unblocked:
  - the official ND contour service is confirmed queryable
  - the new recursive batch-splitting logic is working through its `HTTP 500` batch-size limits
  - full ND ingestion is still in progress / not yet published
- Expanded the acquired-but-not-live coverage lane:
  - Wisconsin hypsography ZIP is valid after all and contains `342` per-lake CSVs
  - extracted `/Users/Ashar/Documents/fish/data/bathymetry/wi/wi_hypsography_summary.csv` with lake code, max depth, and depth-level count for all `342` files
  - New Brunswick full bathymetry sounding pull completed: `18,020` depth points at `/Users/Ashar/Documents/fish/data/bathymetry/nb/nb_bathy_full.geojson`
  - Nova Scotia official open-data pull completed: `1,194` surveyed lake footprints at `/Users/Ashar/Documents/fish/data/bathymetry/ns/ns_lake_survey_points.geojson`
- Extended `normalize_survey_geojson.py` to support:
  - `nb` as `depth_points`
  - `ns` as `survey_footprint`
- Normalized both new Canadian sources into the shared manifest:
  - `nb_normalized.geojson` with `18,020` normalized features
  - `ns_normalized.geojson` with `1,194` normalized features
- Updated `/Users/Ashar/Documents/fish/docs/us-canada-bathymetry-coverage-matrix.md` so Wisconsin, New Brunswick, and Nova Scotia are now tracked as raw-acquired / usable sources rather than just “identified”.
- Continued the expansion sweep beyond the first blocker batch:
  - Maine KML was converted into `/Users/Ashar/Documents/fish/data/bathymetry/me/me_regions.geojson` with `68` survey-region index features
  - Maine is now normalized through `normalize_survey_geojson.py` as a `survey_index` lane (`me_normalized.geojson`)
  - Arkansas ScienceBase inventory was expanded into `/Users/Ashar/Documents/fish/data/bathymetry/ar/ar_sciencebase_inventory.csv` and one real USGS geodatabase was downloaded: `nimrod_2017.gdb.zip` (~`589 MB`)
  - Missouri ScienceBase inventory was expanded into `/Users/Ashar/Documents/fish/data/bathymetry/mo/mo_sciencebase_inventory.csv` and one real bathymetry ZIP was downloaded: `clearwater_mb_1m_pts.zip` (~`213 MB`)
  - Updated the coverage matrix so `ME`, `AR`, and `MO` now reflect real acquired artifacts on disk instead of only vague source leads
- North Dakota improved materially:
  - `nd_full_contours.geojson` on disk now contains about `4,705` contour features (close to the expected `4,765`)
  - `download_all_states.py` was patched so a failed object-id batch no longer aborts the entire state download
  - ND normalization was started and is producing `nd_normalized.geojson` / `nd_labels.geojson`; tile build was still running at handoff time
- Nebraska local artifact recovery was retried and still failed with the current direct download URL (`HTTP 400`), so the local repo still lacks a clean `ne` source file even though the live app/server already has Nebraska coverage.
- Later in the day, Nebraska was fully recovered locally from the live ArcGIS service:
  - `https://services5.arcgis.com/IOshH1zLrIieqrNk/ArcGIS/rest/services/LakeContours/FeatureServer/0/query`
  - downloaded `4,280` contour features into `/Users/Ashar/Documents/fish/data/bathymetry/ne/ne_contours.geojson`
  - normalized them into the shared schema and built `/Users/Ashar/Documents/fish/infra/martin/tiles/ne_contours.pmtiles`
- Wisconsin was promoted from a dead hypsography lead into a real live lane:
  - joined `342` Wisconsin hypsography lake summaries to official DNR hydro polygons
  - wrote `/Users/Ashar/Documents/fish/data/bathymetry/wi/wi_hypsography_polygons.geojson`
  - normalized to `wi_normalized.geojson`
  - built `/Users/Ashar/Documents/fish/infra/martin/tiles/wi_contours.pmtiles`
- `build_survey_pmtiles.py` was patched so explicitly selected sources can be built even if `tile_ready` is false. That unlocked on-demand promotion of summary/point/index lanes like `wi`, `nb`, `ns`, and `me`.
- Built new local PMTiles:
  - `nd_contours.pmtiles` (~`41 MB`)
  - `wi_contours.pmtiles` (~`3.4 MB`)
  - `nb_contours.pmtiles` (~`2.2 MB`)
  - `ns_contours.pmtiles` (~`16 MB`)
  - `me_contours.pmtiles` (~`0.2 MB`)
- Wired the app/runtime for the new non-contour lane types:
  - `lake_summary` now renders as max-depth lake shading
  - `depth_point` now renders as sounding dots/labels
  - `survey_footprint` and `survey_index` now render as subtle coverage outlines
  - added `WI`, `NB`, `NS`, and `ME` to `contourMapService.ts`
- Published the new batch to the live Martin server:
  - synced `ND/WI/NB/NS/ME` PMTiles to `24.199.80.77`
  - updated remote Martin config to include them
  - verified public TileJSON endpoints respond for `nd_contours`, `wi_contours`, `nb_contours`, `ns_contours`, `me_contours`, and `ne_contours`
- Important operational lesson:
  - restarting `docker compose` on the droplet without `--env-file .env.prod` recreated `db` and `martin` with blank credentials, breaking Martin DB auth
  - fixed by recreating with `docker compose -f docker-compose.prod.yml --env-file .env.prod up -d db martin`
  - future remote compose commands on that host must always include `--env-file .env.prod`
- Manitoba and Saskatchewan were fully promoted from local survey-index blockers into live app lanes:
  - fixed `sk` empty-point normalization by validating coordinates before writing features
  - fixed `mb` point geometry by reprojecting the source Web Mercator coordinates to WGS84 during normalization
  - built `mb_contours.pmtiles` (~`1.6 MB`) and `sk_contours.pmtiles` (~`1.5 MB`)
  - published both to the live Martin server and verified direct TileJSON responses from `/mb_contours` and `/sk_contours`
- Arkansas was promoted from a USGS geodatabase artifact into a real live contour source:
  - installed a lightweight local GIS stack (`pyogrio`, `geopandas`, `pyproj`)
  - extracted the `Contours` layer from `nimrod_2017.gdb.zip` into `/Users/Ashar/Documents/fish/data/bathymetry/ar/ar_contours.geojson`
  - added `ar` to `normalize_survey_geojson.py` as a tile-ready contour source
  - normalized `1,252` contours + `1,252` labels and built `/Users/Ashar/Documents/fish/infra/martin/tiles/ar_contours.pmtiles` (~`2.8 MB`)
  - added `ar_contours` to the app source registry and the live Martin config, then verified `/ar_contours` publicly
- Missouri Clearwater Lake work started in earnest:
  - added `/Users/Ashar/Documents/fish/ml/bathymetry/extract_mo_clearwater_depth_points.py`
  - the extractor downscales the `18.9M`-point `clearwater_mb_1m_pts.zip` survey into a tileable sounding lane by computing depth from water-surface elevation and aggregating onto a projected grid
  - the reduction finished with `3,175` depth-point features in `/Users/Ashar/Documents/fish/data/bathymetry/mo/mo_clearwater_depth_points.geojson`
  - `mo` was added to `normalize_survey_geojson.py` as a depth-point source, normalized cleanly, built into `/Users/Ashar/Documents/fish/infra/martin/tiles/mo_contours.pmtiles`, and published live
- End-of-pass live verification:
  - `ar_contours`, `mo_contours`, `mb_contours`, and `sk_contours` all return public TileJSON successfully from `http://24.199.80.77`
  - honest inland coverage after this pass:
    - survey-backed contour jurisdictions: `18 / 63` (`28.6%`)
    - jurisdiction-specific live inland lanes of any kind: `26 / 63` (`41.3%`)
    - any live inland layer at all including coarse US fallback: `55 / 63` (`87.3%`)
- Final North America baseline inland coverage push:
  - recovered and published Alaska from the live ADF&G bathymetry service:
    - downloaded `998` bathymetry polygons
    - normalized + tiled into `ak_contours.pmtiles`
    - verified public `200` from `/ak_contours`
  - added HydroLAKES-based coarse fallback lanes for the last uncovered jurisdictions:
    - Hawaii (`hi_contours`)
    - Newfoundland & Labrador (`nl_contours`)
    - Prince Edward Island (`pe_contours`)
    - Yukon (`yt_contours`)
    - Northwest Territories (`nt_contours`)
    - Nunavut (`nu_contours`)
  - tuned the HydroLAKES fallback builder for northern territories:
    - pushed country/depth filters down into the GDAL read
    - added coarse lake-area thresholds (`NT >= 1 km²`, `NU >= 10 km²`)
    - simplified point-mode territory clipping geometry for coarse fallback lanes
    - switched GeoJSON writes to `pyogrio.write_dataframe`, which removed the slow Fiona write bottleneck
  - results from the final territory/coarse pass:
    - `nt_hydrolakes.geojson`: `15,361` point features after jurisdiction filtering
    - `nu_hydrolakes.geojson`: `1,551` major-lake point features after jurisdiction filtering
    - `nt_contours.pmtiles`: ~`2.0 MB`
    - `nu_contours.pmtiles`: ~`0.8 MB`
  - published `AK/HI/NL/PE/YT/NT/NU` to the live Martin server and verified public `200` responses for:
    - `/ak_contours`
    - `/hi_contours`
    - `/nl_contours`
    - `/pe_contours`
    - `/yt_contours`
    - `/nt_contours`
    - `/nu_contours`
  - updated the coverage matrix so the honest inland baseline is now:
    - survey-backed contour jurisdictions: `20 / 63` (`31.7%`)
    - jurisdiction-specific live inland lanes of any kind: `34 / 63` (`54.0%`)
    - any live inland layer at all across U.S. + Canada inland coverage: `63 / 63` (`100%`)
  - key distinction:
    - `100%` now means every U.S. state and Canadian province/territory has *some* live inland bathymetry lane
    - it does **not** mean survey-grade or Navionics-like contour quality everywhere yet
- Final publish blocker resolved:
  - the public nginx edge was caching `/catalog` for `6h`, so newly published Martin sources appeared missing even when their files and direct endpoints were healthy
  - added an uncached `location = /catalog` block to `/Users/Ashar/Documents/fish/infra/nginx/nginx.prod.conf`
  - synced the config to the droplet, restarted nginx, and verified the public catalog now includes `nt_contours` and `nu_contours`
- Texas quality upgrade moved from blocker to live pilot:
  - fixed `scrape_tx_twdb_inventory.py` so recalculated TWDB survey links are keyed by lake rather than by survey date folder
  - built `/Users/Ashar/Documents/fish/ml/bathymetry/fetch_tx_twdb_surveys.py` to select and download the best TWDB shapefile bundle per lake
  - built `/Users/Ashar/Documents/fish/ml/bathymetry/extract_tx_twdb_contours.py` to pull the correct contour layer out of TWDB ZIPs, reproject it, and derive `DEPTH_FT` from lake surface elevation minus contour elevation
  - validated the first Texas pilot on Bardwell Lake:
    - `144` contour features extracted
    - derived depth range `0.98–32.98 ft`
    - normalized as `tx_twdb_survey`
    - built `/Users/Ashar/Documents/fish/infra/martin/tiles/tx_contours.pmtiles` (~`1.2 MB`)
  - published `tx_contours` to the live Martin server and verified public `200` responses from:
    - `http://24.199.80.77/tx_contours`
    - `http://24.199.80.77/tx_contours/10/236/414`
  - updated the honest inland coverage summary:
    - survey-backed contour jurisdictions: `21 / 63`
    - jurisdiction-specific live inland lanes of any kind: `35 / 63`
    - any live inland layer across U.S. + Canada inland coverage: still `63 / 63`
- Fixed the biggest remaining ML contour fallback gap in `/Users/Ashar/Documents/fish/ml/bathymetry/production_depth_router.py`:
  - the old `get_lake_map()` ML-only branch returned an all-`NaN` placeholder grid with a TODO, so unsurveyed lakes could not actually emit ML-derived contour surfaces
  - replaced that placeholder with a real fallback surface builder that:
    - classifies lakes into `glacial_model`, `mountain_model`, or `unified_fallback`
    - uses cached point features when available
    - otherwise synthesizes a shoreline-anchored depth surface from lake geometry + morphometric max-depth priors
    - also uses the same fallback path to fill survey gaps
  - added source metadata / RMSE / attribution entries for the regional ML fallback families so downstream contour renderers can style them correctly
  - hardened lake mask rasterization so the fallback path still works in lean dev environments even when `rasterio` is missing locally
- Expanded the live Texas TWDB survey lane beyond the original Bardwell pilot:
  - extracted a new three-lake contour set for Bridgeport (`1,825` contours), Cherokee (`742`), and Conroe (`2,341`)
  - normalized Texas into `4,908` contour features and rebuilt the tile into a `20.5 MB` PMTiles archive with `6,454` derived depth bands (`16,270` combined features)
  - published the refreshed Texas tile to the live Martin host and verified real public vector-tile content for Bridgeport, Cherokee, and Conroe, even though Martin still reports stale Bardwell-only TileJSON metadata for `tx_contours`
  - widened the app registry bounds for Texas to the real multi-lake footprint (`-98.01,30.35` → `-94.60,33.31`)
- Added an app-side safeguard against stale Martin TileJSON metadata:
  - inland bathymetry `VectorSource`s in `MapScreen.tsx` now mount via direct `tileUrlTemplates` from a new `buildTileTemplateUrl()` helper instead of relying on source-level TileJSON metadata
  - this keeps live multi-lake Texas coverage usable in the app even while Martin's `tx_contours` TileJSON header is lagging behind the hot-swapped PMTiles content
- Freed local disk safely to unblock the Texas rebuild:
  - offloaded the unused Texas raw survey bundles for Buchanan, Big Creek, and Eagle Mountain to `/opt/opencatch/backups/tx-raw` on the droplet and removed the local copies
  - recovered local free space from ~`0.2 GB` to ~`3.5 GB`
