Mobile bathymetry app fix:
- Unified the live inland contour path around Martin-served vector sources.
- Patched release networking so the Expo app can fetch contour tiles from the live tile server while tiles.opencatch.app is unresolved.
- Added inland contour depth labels and tap-to-depth/lake attribution card in MapScreen.
- Replaced stale PMTiles-oriented contourMapService with Martin-aligned shared helpers/constants.
- Full mobile typecheck still fails on an older pre-existing MapScreen issue: dynamicDepthLoading referenced before declaration.
- Removed risky native contour label/dash styling after guest-entry crash in release build; kept fills, lines, and tap-to-depth.

Survey contour ingestion/tile pipeline:
- Replaced the brittle chunked GeoJSON decoder in `normalize_survey_geojson.py` with a stateful feature-array streaming parser.
- Added manifest-safe incremental reruns so normalizing one source no longer wipes the whole manifest.
- Added Vermont as a new tile-ready survey source.
- Added `build_survey_pmtiles.py` to combine normalized contours + labels into Martin-ready PMTiles.
- Built local PMTiles successfully for Alberta (`ab_contours.pmtiles`), Vermont (`vt_contours.pmtiles`), and Michigan (`mi_contours.pmtiles`).
- Florida normalization completed cleanly at `24,311` contour features and `24,290` labels; Florida PMTiles build started afterward.
- Confirmed Ontario, Quebec, Washington, and Massachusetts raw archives are readable via GDAL/OpenFileGDB/shapefile tooling, so they are next parsing targets.
- Code/docs commit for parser + tiling work: `e5f0c68` on `deploy-clean`, pushed to origin.
