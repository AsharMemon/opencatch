# 2026-03-24

- Continued bathymetry work on branch `codex/improvement`.
- Finished `hybrid_phys` run on `/data/codex_bathy_20260324T010114Z/hybrid_phys`.
  - Best finished metric there: `clarity_blend` with `RMSE 4.386 m`, `R² 0.391`.
- Implemented a stronger `train_hybrid_sonar.py` variant with:
  - lake-relative spatial features from sample geometry
  - similar-lake donor priors from train lakes
  - blended stage-1 priors using donor + model predictions
  - range-aware expert branch
  - meta-stacking and optional isotonic calibration
- Smoke-tested the new trainer remotely on a 100-lake subset using `train_hybrid_sonar_vnext.py`; completed cleanly.
- Launched full vnext run interactively on Vast at:
  - experiment root: `/data/codex_bathy_20260324T063400Z_vnext`
  - output dir: `/data/codex_bathy_20260324T063400Z_vnext/hybrid_full`
- Full vnext run completed with best test metric:
  - `calibrated_meta`: `RMSE 4.2056 m`, `R² 0.4402`
  - raw `stacked_meta`: `RMSE 4.2107 m`, `R² 0.4388`
  - `clarity_blend`: `RMSE 4.2059 m`, `R² 0.4401`
- Compared with prior full best (`4.3851 m / 0.3913`), vnext improved by about:
  - `0.18 m` lower RMSE
  - `+0.049` R²
- New full-run bottleneck is still depth-range compression:
  - shallow bins still overpredicted by about `+2.24 m`
  - `20–50 m` depths still underpredicted by about `-10.08 m` for `clarity_blend`
- Added a separate Payandeh et al. (2026)-style replication path:
  - script: `ml/bathymetry/replicate_payandeh_2026.py`
  - runbook: `ml/bathymetry/PAYANDEH_2026_REPLICATION.md`
  - supports ATL24 parquet or live SlideRule ATL24 query, STAC Sentinel-2 or local manifest, whole-track holdout, per-scene metrics, mean-map averaging, Gaussian smoothing
  - synthetic smoke test completed successfully at `/tmp/payandeh_demo_codex/metrics.json`
  - remote real-data smoke test also completed on Vast using an approximate Langebaan bbox with live ATL24 + STAC Sentinel-2:
    - `186` ATL24 points across `11` tracks
    - `2` Sentinel-2 scenes resolved
    - first naïve paper-style replication was poor: best scene `RMSE 9.60 m`, `R² -0.55`
    - this is useful evidence that simply copying the paper feature set + XGBoost does not reproduce their headline result in our first pass
  - second remote smoke with stricter ATL24 filtering and `60`-day scene-date tolerance:
    - ATL24 filtering reduced `186 -> 169` rows and `11 -> 9` tracks
    - `4` candidate scenes, `3` usable scenes after pairing
    - best scene improved to `RMSE 1.51 m`, but `R²` stayed very negative because the held-out set collapsed to a single shallow track with only ~`1.45-2.51 m` depth range
    - lesson: tighter temporal matching can improve RMSE materially, but needs either true ACOLITE inputs or a scene-specific track split to avoid degenerate `R²`
- Hardened `ml/bathymetry/replicate_payandeh_2026.py` further:
  - added scene-aware split fallback when date filtering leaves a scene with a degenerate holdout
  - added aggregated held-out point metrics across scenes
  - added depth-stratified metrics (`0-1m`, `1-2m`, `2-5m`, `5-10m`, etc.)
  - added optional `--feature-set extended` with raw bands plus simple spatial priors (`shore_dist_m`, `row_norm`, `col_norm`)
- Installed `optuna` on the active Vast box so the paper-style path now uses Bayesian-style tuning instead of random-search fallback.
- New honest replication sweeps on Vast over the approximate Langebaan AOI:
  - strict paper mode, `max_depth=30`, `max_scenes=6`, `scene_date_tolerance=60d`:
    - aggregated held-out points: `RMSE 6.851 m`, `R² 0.218`
    - still far from paper headline metrics
  - strict paper mode, `max_depth=5`, `max_scenes=6`:
    - aggregated held-out points: `RMSE 0.393 m`, `R² -0.032`
    - depth bins:
      - `1-2m`: `RMSE 0.359 m`
      - `2-5m`: `RMSE 0.449 m`
    - this is the first honest sub-meter result in the replication track
  - extended feature mode, `max_depth=5`, `max_scenes=6`:
    - aggregated held-out points: `RMSE 0.412 m`, `R² -0.131`
    - slightly worse than strict paper features
  - strict paper mode, `max_depth=10`, `max_scenes=6`:
    - aggregated held-out points: `RMSE 1.647 m`, `R² -0.017`
    - depth bins:
      - `1-2m`: `RMSE 0.437 m`
      - `2-5m`: `RMSE 0.701 m`
      - `5-10m`: `RMSE 4.285 m`
    - conclusion: the setup still breaks badly in the `5-10m` band
  - extended feature mode, `max_depth=10`, `max_scenes=6`:
    - aggregated held-out points: `RMSE 1.700 m`, `R² -0.097`
    - worse than strict paper features again
- A final `max_depth=5`, `max_scenes=16` paper-style run was launched to check whether more scenes and stronger scene averaging can push the shallow benchmark lower than `0.393 m`.
